# 2025-06-03 工作日志

## 考勤监控 - 添加管理后台 + 强制推送开关

### 改动内容
- 在 `index.js` 中集成 HTTP 管理后台（端口 3456）
- 新增全局状态 `appState`：forceMode、forceIntervalSeconds、lastRunTime 等
- 守护循环从固定等待改为 5 秒短轮询，支持运行时动态切换间隔
- 强制模式下跳过数据变化检测，无条件推送
- 管理页面：深色主题，含开关（强制推送）+ 数字输入（推送间隔秒数，范围 5-3600）
- API 端点：GET /api/status、POST /api/toggle、POST /api/interval

### 部署
- 已同步到服务器并重启 PM2
- 已开放 UFW 3456 端口（腾讯云安全组需额外配置）
- 访问方式：SSH 隧道或直接开放安全组

### 管理后台地址
- http://106.52.246.40:3456/ （需安全组放行）
- 本地隧道：ssh -L 3456:localhost:3456 ubuntu@106.52.246.40
