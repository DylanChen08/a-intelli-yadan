# 2026-06-04 工作日志

## 考勤监控 - 添加下班实时监听 + 全屏提示 + UI 细节调整

### 改动内容
- `appState` 新增 `yadanLeaveTime`（HH:mm:ss）和 `yadanLeaveDate`（YYYY-MM-DD）字段
- `runOnce` 每次查询后更新雅丹下班状态，跨天自动重置
- `/api/status` 接口增加 `yadanLeaveTime` / `yadanLeaveDate` 字段
- 管理页面新增「👀 下班监听」开关（橙色 toggle）
- 开关开启后页面每 5 秒轮询 API，一旦检测到雅丹下班时间出现（或变化），触发全屏橙色渐变覆盖层 + 跳动 emoji + 大字提示
- 同一下班时间只触发一次，点击任意位置关闭覆盖层

### 访问地址
- http://106.52.246.40/admin

## 考勤监控 - 管理页面 UI 细节更新（二次迭代）
- 「雅丹状态」改名为「🥚 鸭蛋状态」，措辞改为「鸭蛋已下班 / 鸭蛋上班中」
- 状态栏顶部新增实时时钟（`id="clock"`），每秒更新，格式 `YYYY年MM月DD日 HH:mm:ss`，使用本地时区

## 考勤监控 - React 18 重写管理页面（三次迭代）
- 管理页面从原生 JS + 模板字符串 → React 18 + htm（CDN 加载，零构建）
- 页面抽成独立 `admin.html` 文件，`index.js` 改用 `readFileSync` 读取
- 组件拆分：`Toggle`（开关）、`LeaveAlert`（全屏提示）、`StatusBar`（状态栏）、`App`（根组件）
- 所有功能保持不变：强制推送、推送间隔、下班监听、实时时钟、鸭蛋状态、全屏提示

## 考勤监控 - 修复 React #31 错误（四次迭代）
- htm 修补未能彻底解决，根本原因是 React 18 + htm 模板解析存在兼容边界
- 最终方案：**彻底移除 htm 依赖**，改用纯 `React.createElement`（别名为 `h`）重写全部组件
- 所有功能保持不变，运行时从 3 个 CDN 请求减少到 2 个（移除 htm）

## 考勤监控 - 回退到纯原生 JS（五次迭代）
- 用户决定不用 React，彻底移除 React/htm 全部 CDN 依赖
- 恢复为纯原生 JS + DOM 操作，零外部运行时依赖
- 所有功能完全保留：强制推送、推送间隔、下班监听、实时时钟、鸭蛋状态、全屏提示
